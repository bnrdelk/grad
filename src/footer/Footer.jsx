const Footer = () => {
    return (
        <footer className="bottom-0 left-0 w-full text-center text-gray-500 p-2">
            <p>© {new Date().getFullYear()} Nailart</p>
            <a href="#">.</a>
        </footer>
    );
};

export default Footer